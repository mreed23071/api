"""Wire contracts published by API v1.

Once v1 ships, these shapes are frozen: additive changes only. A breaking change
means a new version package, not an edit here. Domain DTOs under
`app.domains.*.dto` carry no such promise - that asymmetry is the entire point
of keeping them apart.
"""
