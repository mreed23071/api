"""Identity context.

Owns the answer to "who is this person, and which third-party accounts are
theirs". Publishes `Platform`, `User`, `UserRelation`, `IdentityCandidate` and
`IdentityService`; everything else here is internal.
"""
